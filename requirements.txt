# ============================================================
# Rondônia SR Study – Python Dependencies  (v2)
# ============================================================
# Core deep-learning
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Transformer attention layer helpers
timm>=0.9.12

# Geospatial / raster I/O
rasterio>=1.3.9
numpy>=1.24.0
scipy>=1.11.0

# Image registration & quality metrics
scikit-image>=0.22.0
lpips>=0.1.4

# SAM (Spectral Angle Mapper) and PSNR/SSIM
piq>=0.8.0

# Downstream segmentation task (U-Net F1/IoU metrics)
scikit-learn>=1.3.0     # F1-score, confusion matrix
torchmetrics>=1.2.0     # clean metric accumulation

# General ML utilities
tqdm>=4.66.0
pandas>=2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
tabulate>=0.9.0

# Config management
pyyaml>=6.0.1

# Data download (Microsoft Planetary Computer)
planetary-computer>=1.0.0
pystac-client>=0.7.0
stackstac>=0.4.4

# ─────────────────────────────────────────────────────────────
# Cloud execution notes:
#   Kaggle:       pip install -r requirements.txt (pre-installed: torch, numpy, pandas)
#   Saturn Cloud: conda env from requirements.txt is supported
# Install command (all platforms):
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────
